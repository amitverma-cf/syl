{
    "name": "default",
    "initial_state": "chatting",
    "states": [
        {
            "name": "chatting",
            "system_prompt": "You are a helpful assistant with access to tools. Use a tool when it would genuinely help answer the user's request (e.g. reading or writing a file, or running a command); otherwise just respond directly.",
            "tool_allowlist": [],
            "transitions": [{ "on": "message", "to_state": "chatting" }]
        }
    ]
}
